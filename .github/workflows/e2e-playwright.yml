name: Run Playwright E2E (placeholder)

on:
  workflow_dispatch:

jobs:
  e2e-placeholder:
    name: Playwright E2E (placeholder)
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "This workflow was preserved as a placeholder."
name: Run Playwright E2E (placeholder)

on:
  workflow_dispatch:

jobs:
  e2e-placeholder:
    name: Playwright E2E (placeholder)
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "This workflow was preserved as a placeholder."
name: Run Playwright E2E (placeholder)

on:
  workflow_dispatch:

jobs:
  e2e-placeholder:
    name: Playwright E2E (placeholder)
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "This workflow was preserved as a placeholder."
name: Run Playwright E2E

on:
  pull_request:
    branches: [ master ]
  push:
    branches: [ 'feat/**', master ]

jobs:
  e2e:
    name: Playwright E2E
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Python requirements
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt || true

      name: Run Playwright E2E

      on:
        workflow_dispatch:

      jobs:
        e2e-placeholder:
          name: Playwright E2E (placeholder)
          runs-on: ubuntu-latest
          steps:
            - name: Placeholder
              run: echo "This workflow was preserved as a placeholder. The real job runs in `ci.yml`."
            echo "waiting for backend... ($i)"; sleep 2
